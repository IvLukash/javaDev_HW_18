CREATE TABLE IF NOT EXISTS note (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    title VARCHAR(100) NOT NULL,
    content TEXT NOT NULL,

    CONSTRAINT ck_title_length CHECK (LENGTH(title) BETWEEN 3 AND 100)
);